apply plugin: 'com.android.application'

android {
    compileSdk 35
    namespace "com.genymobile.scrcpy"
    defaultConfig {
        applicationId "com.genymobile.scrcpy.v1"
        minSdkVersion 24
        targetSdkVersion 35
        versionCode 25
        versionName "2.5"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }

    task copyRelease(dependsOn: 'deleteServer', type: Copy) {
        from file('build/outputs/apk/release/scrcpy-release-unsigned.apk')
        into file('../screen/src/main/assets/')
        rename('scrcpy-release-unsigned.apk', 'scrcpy-server.jar')
    }

    task deleteServer(type: Delete) {
        delete "../screen/src/main/assets/scrcpy-server.jar"
    }


    task copyDebug(dependsOn: 'deleteServer', type: Copy) {
        from file("build/outputs/apk/debug/scrcpy-debug.apk")
        into file("../screen/src/main/assets/")
        rename('scrcpy-debug.apk', 'scrcpy-server.jar')
    }

    afterEvaluate {
        packageRelease.finalizedBy(copyRelease)
    }

    afterEvaluate {
        packageDebug.finalizedBy(copyDebug)
    }
    buildFeatures {
        aidl true
        buildConfig true
    }
}


dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    testImplementation 'junit:junit:4.13.2'
}

apply from: "$project.rootDir/config/android-checkstyle.gradle"

